server:
  port: 8761

spring:
  application:
    name: eureka-server
  cloud:
    config:
      enabled: false
#      uri: http://localhost:8888
      label: master
#      name: eureka-server
      profile: dev
      fail-fast: true
      discovery:
        enabled: false
        service-id: config-server



